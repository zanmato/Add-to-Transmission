#!/bin/bash

# Build config for the build script, build.sh. Look there for more info.

APP_NAME=addtotransmission
CHROME_PROVIDERS="content locale defaults"
CLEAN_UP=1
ROOT_FILES=
ROOT_DIRS=
BEFORE_BUILD=
AFTER_BUILD=
